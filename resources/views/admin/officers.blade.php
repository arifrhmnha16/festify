@extends('layouts.admin', ['title' => 'Kelola Petugas', 'pageTitle' => 'Kelola Petugas'])
@section('content')
<section><form method="post" action="{{ route('admin.officers.store') }}" class="grid gap-3 rounded-lg border bg-white p-5 md:grid-cols-5">@csrf<input name="name" placeholder="Nama" class="rounded border px-3 py-2"><input name="username" placeholder="Username" class="rounded border px-3 py-2"><input name="password" placeholder="Password" class="rounded border px-3 py-2"><select name="role" class="rounded border px-3 py-2"><option value="loket">Loket</option><option value="gate">Gate</option></select><button class="rounded bg-neutral-950 px-4 py-2 font-bold text-white">Tambah</button></form><div class="mt-6 grid gap-3">@foreach($officers as $officer)<div class="rounded-lg border bg-white p-4"><form method="post" action="{{ route('admin.officers.update',$officer) }}" class="grid gap-2 md:grid-cols-5">@csrf @method('put')<input name="name" value="{{ $officer->name }}" class="rounded border px-3 py-2"><input name="username" value="{{ $officer->username }}" class="rounded border px-3 py-2"><input name="password" placeholder="Password baru" class="rounded border px-3 py-2"><select name="role" class="rounded border px-3 py-2"><option value="loket" @selected($officer->role==='loket')>Loket</option><option value="gate" @selected($officer->role==='gate')>Gate</option></select><button class="rounded bg-orange-700 px-4 py-2 font-bold text-white">Simpan</button></form><form method="post" action="{{ route('admin.officers.destroy',$officer) }}" class="mt-2">@csrf @method('delete')<button class="text-sm font-bold text-red-700">Hapus Petugas</button></form></div>@endforeach</div><div class="mt-6">{{ $officers->links() }}</div></section>
@endsection
